<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 语音输入需要录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- ML Kit 模型下载等网络能力（内置模型不强制） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络状态监听（网络恢复自动同步） -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".SmartButlerApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_mascot"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartButler"
        android:usesCleartextTraffic="true">

        <!-- 启动页：登录（已登录自动跳转主界面） -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.voice.VoiceInputActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ocr.OcrInputActivity"
            android:exported="false" />

        <activity
            android:name=".ui.community.MyPostsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />
    </application>
</manifest>
