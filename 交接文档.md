# 交接文档（第 5 位同学）

> 你好！接下来由你负责 **UI 美化 + 吉祥物**。以下是项目交接说明，请按顺序操作。

## 1. 拉取代码

```bash
git pull
```

## 2. 打开项目

用 Android Studio 打开 **SmartButler** 文件夹。

## 3. 配置 JDK

项目使用 **JDK 17**，已在 `gradle.properties` 中配置，无需额外操作。

## 4. 连接服务器

打开 App →「我的」→「服务器设置」→ 输入：

```
http://10.126.126.1:8000
```

## 5. 测试账号

| 用户名 | 密码 |
| --- | --- |
| test1 | 123456 |
| test2 | 123456 |

## 6. 你需要修改的 UI 资源

详见同目录下的 **[UI资源清单.md](./UI资源清单.md)**，主要包括：

- 颜色（`res/values/colors.xml`）
- 主题（`res/values/themes.xml`）
- 吉祥物（`res/drawable/ic_mascot.xml`）
- 布局（`res/layout/*.xml`）
- 图标（`res/drawable/ic_*.xml`）
- 文案（`res/values/strings.xml`）

## 7. 提交代码

```bash
git add .
git commit -m "UI美化: xxx"
git push
```

> 提交信息请按实际修改内容填写，如 `UI美化: 统一配色与吉祥物`。

## 8. 答辩前检查

- [ ] 所有页面配色统一
- [ ] 吉祥物出现在关键位置
- [ ] 各页面无白底白字 / 黑底黑字
- [ ] 录屏备用

---

## 项目背景速览

- **Android 端**：Kotlin + Room + Retrofit + Vosk（语音）+ ML Kit（OCR）
- **后端**：Python FastAPI + SQLite + JWT（`backend/` 目录）
- **架构**：传统 Activity/Fragment + View Binding，非 Compose / Hilt / ViewModel
- **主要功能**：语音/OCR/手动记账、图表统计、预算规划、余额预警、社区、账号系统
- 修改 UI 时请保持 Java 17 + Kotlin 1.9 兼容，勿引入新的大依赖

---

## 本轮改动记录（UI 美化 + 吉祥物小鸥）

### 1. 主题与配色
- **改为清新浅色系**：`SmartButlerApp.kt` 中 `MODE_NIGHT_NO`（原为强制深色）。
- 主色海鸥青绿 `#14B8A6`，品牌渐变 `#2DD4BF → #0E7490`，背景 `#F4F7FB`，白卡圆角。
- 全部颜色集中在 `res/values/colors.xml`，页面自动跟随，无需逐页改。

### 2. 吉祥物「小鸥」
- `res/drawable/` 下 4 款矢量形象：
  - `ic_mascot.xml`（默认·戴耳机）
  - `ic_mascot_happy.xml`（开心·记账成功）
  - `ic_mascot_alert.xml`（提醒·余额预警）
  - `ic_mascot_hat.xml`（换装·渔夫帽）
- 新增 `util/MascotManager.kt`：单例管理当前形象，`SharedPreferences` 持久化，
  提供 `switchMascot()` / `observe()`（页面随换装即时刷新）。
- 出现位置：首页统计卡右上、我的页大头像、登录/注册页、预警横幅、三处空状态、社区空状态。
- **换装入口**：「我的」→「小鸥换装」弹出形象选择框（2 列网格，已实现 4 款切换）。

### 3. 扩展预留
- `MascotManager.MascotSkin` 已预留「装扮」概念；新增形象只需在 drawable 加资源 + 枚举登记。
- 记账成功 Toast、预警表情均已联动（`AddTransactionDialog`、`AlertFragment`）。

### 4. 环境注意（本机）
- `gradle.properties` 中 `org.gradle.java.home=C:/Android/jdk-17`（Gradle 8.2 不支持 Android Studio JBR 25）。
- 项目路径含中文，已在 gradle.properties 添加 `android.overridePathCheck=true`。
- 编译验证：`gradlew :app:assembleDebug` 通过。
